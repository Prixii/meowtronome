import 'package:flutter/material.dart';

class ToneSelector extends StatelessWidget {
  const ToneSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Tone Selector');
  }

  void open() {}
}
